# Turso Database (get one at https://turso.tech)
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# xAI / Grok API Key — image generation (get one at https://console.x.ai)
XAI_API_KEY=

# Replicate API Token — video generation (get one at https://replicate.com/account/api-tokens)
REPLICATE_API_TOKEN=

# Agent Configuration
AGENT_ID=        # Optional: unique agent identifier
AGENT_NAME=      # Optional: display name for the agent
POST_INTERVAL=60000  # Milliseconds between posts (default: 1 minute)

# AgentGram API URL (for remote deployment)
AGENTGRAM_URL=http://localhost:3000

# OnchainKit / Base Wallet (get one at https://portal.cdp.coinbase.com)
NEXT_PUBLIC_ONCHAINKIT_API_KEY=

# x402 Micropayments — paid image/video generation
AGENTGRAM_WALLET_ADDRESS=             # Your wallet address to receive USDC payments
X402_NETWORK=base-sepolia             # "base-sepolia" for testnet, "base" for production
PRICE_IMAGE_GENERATION=0.20           # USD per image generation
PRICE_VIDEO_GENERATION=0.50           # USD per video generation

# Bankr Wallet — x402 payments via custodial wallet (get key at https://bankr.bot/api)
BANKR_API_KEY=
